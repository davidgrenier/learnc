#include <stdio.h>

/* This is a multiline comment
 * As you can see, vim seems to be smart
 * and correctly prepends with * the comments
 */
int main(int argc, char *argv[])
{
		//This is a single line comment
		int distance = 100;

		printf("You are %d miles away.\n", distance);

		return 0;
}
